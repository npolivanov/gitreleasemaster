## План: Кнопка добавления в «Создать шаблон» — только иконка

### Изменение
**`commits/unstaged/template-tab/template-tab.tsx`**:
- Убрать текстовую кнопку «Добавить» (`variant="contained"` с подписью).
- Вместо неё — `IconButton` с иконкой `AddIcon` (size small) и tooltip «Добавить в ручное добавление релиза»; `disabled` при пустом результате.
- Поведение то же: клик → `addMany(result)` → поле «Текст» очищается, вкладка переключается на «Ручное добавление релиза» (обёртка в `unstaged.tsx` не меняется).
- Почистить импорты: `Button` больше не нужен, добавить `IconButton`.

### Сборка
`npm run build-webview` + Reload Window.

### Итог по файлам
| Файл | Действие |
|---|---|
| `commits/unstaged/template-tab/template-tab.tsx` | Кнопка «Добавить» → IconButton с иконкой (+) и tooltip |